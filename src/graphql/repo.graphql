query repo($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    id
    description
    descriptionHTML
    name
    nameWithOwner
    url
    latestRelease {
      id
      tagName
      publishedAt
      
    }
  }
}